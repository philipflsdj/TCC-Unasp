@model Tcc_Unasp.Models.TipoPagamento

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}
<div class="col-lg-12">
    <div class="card mb-4">
        <div class="card-header">
            <h4 class="m-0 font-weight-bold text-primary">Deletar os Tipos de Pagamento</h4>
        </div>
        <div class="card-body">
            <h3>Tem certeza que você quer deletar o Tipo de Pagamento  @Html.DisplayFor(model => model.Descricao)</h3>
            <div>
                @*<dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.Descricao)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Descricao)
                    </dd>

                </dl>*@

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    <div class="form-actions no-color">
                        <input type="submit" value="Excluir" id="btnCreate" class="btn btn-outline-danger" />
                        <a class="btn btn-outline-danger" href="@Url.Action("Index", "TiposdePagamento")">Voltar para a lista</a>
                    </div>

                    @*<div class="form-actions no-color">
                        <input type="submit" value="Delete" class="btn btn-default" /> |
                        @Html.ActionLink("Back to List", "Index")
                    </div>*@
                }
            </div>

        </div>
    </div>
</div>
 